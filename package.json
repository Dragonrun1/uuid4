{
    "name": "uuid64ts",
    "description": "UUID v4 (random) Typescript/Javascript package with a custom base 64 encoding added.",
    "version": "1.1.8",
    "author": {
        "name": "Michael Cummings",
        "email": "dragonrun1@gmail.com"
    },
    "devDependencies": {
        "@types/chai": "^4.2.11",
        "@types/mocha": "^7.0.2",
        "@types/node": "^12.12.6",
        "chai": "^4.2.0",
        "mocha": "^7.1.1",
        "nyc": "^15.0.0",
        "rimraf": "^3.0.2",
        "ts-node": "^8.6.2",
        "tslib": "^1.11.1",
        "typescript": "^3.8.0"
    },
    "keywords": [
        "UUID",
        "UUIDv4",
        "base64",
        "custom encoding",
        "javascript",
        "random",
        "typescript"
    ],
    "license": "BSD-3-Clause",
    "repository": "https://github.com/Dragonrun1/uuid64ts.git",
    "scripts": {
        "uuid4:clear-test": "rimraf test/**/*.js test/**/*.map test/**/*.d.ts",
        "uuid4:clover": "npm run uuid4:clear-test && nyc --reporter=clover mocha -r ts-node/register test/**/*.spec.ts",
        "uuid4:coverage": "npm run uuid4:clear-test && nyc --reporter=text mocha -r ts-node/register test/**/*.spec.ts",
        "uuid4:html": "npm run uuid4:clear-test && nyc --reporter=html mocha -r ts-node/register test/**/*.spec.ts",
        "uuid4:text-lcov": "npm run uuid4:clear-test && nyc --reporter=text-lcov mocha -r ts-node/register test/**/*.spec.ts | coveralls",
        "start": "rimraf dist/**/* && tsc --watch",
        "test": "npm run uuid4:clear-test && mocha -b -r ts-node/register test/**/*.spec.ts"
    }
}
